import {forwardRef} from "react";
import PropTypes from "prop-types";

const EditTable = forwardRef(({}, ref) => {
    return (
        <div className="table-responsive"></div>
    )
})

EditTable.PropTypes = {
    
}

export default EditTable;